import React from 'react';

export const metadata = {
    title: "Personal Information | CCL",
    description: "Barangay Manaagement System",
  };

  const Layout = ({ children }) => {
    return (
        <>{children}</>
    )
};

export default Layout;