'use client';

import { GlobalError } from '@/components/ui/error';

/**
 * Global error handler for the entire application
 * This component is only rendered when an error occurs in a part of your
 * application where there is no closer error boundary
 */
export default GlobalError; 